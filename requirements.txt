# Python wird nur für tools/ gebraucht (Datengenerierung & Vorberechnung).
# Die Web-App selbst (docs/) hat keine Python-Abhängigkeiten.

pandas>=2.0.0
numpy>=1.24.0

# Vorberechnung für die Module #14 (LOESS), #02/#09/#16 (KDE)
scipy>=1.11.0
statsmodels>=0.14.0

# Vorberechnung für #22 (PCA) und #23 (t-SNE/UMAP)
scikit-learn>=1.3.0
umap-learn>=0.5.5
